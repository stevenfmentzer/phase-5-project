import React from 'react';
import { Card } from 'semantic-ui-react';

function MessageCard(){ 

    return(
        <>
        {/* Show a Card with the message_body, somehow set a left align and right align 
        if sender_id does or doesn't match user_id*/}
        </>
    )
}

export default MessageCard;